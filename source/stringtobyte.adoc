==== StringToByte

(((String Manipulation,StringToByte)))


*Syntax:*
[subs="specialcharacters,quotes"]
----
    var = StringToByte(__string__)   'Supports decimal byte range strings only.

----
*Command Availability:*

Available on all microcontrollers.

*Explanation:*

The `StringToByte` function extracts a number from a string variable and stores it in a byte variable. One potential use is reading numbers that are sent in ASCII format over a serial connection.
{empty} +
{empty} +
`StringToByte` will not extract a value from a hexadecimal string.
{empty} +
{empty} +

*Example 1:*
[source,gcbasic]
----

    ' ----- Configuration
    'Chip Settings.
    #chip 16f18855,32
    #Config MCLRE_ON

    ; ----- Define Hardware settings

    'Set the PPS of the RS232 ports.
    UNLOCKPPS
      RC0PPS = 0x0010     'RC0->EUSART:TX;
      RXPPS  = 0x0011     'RC1->EUSART:RX;
    LOCKPPS

    ; ----- Constants
    #define USART_BAUD_RATE 19200
    #define USART_TX_BLOCKING

    ; ----- Variables
    dim bytevar as Byte

    bytevar = 0

    ; ----- Main body of program commences here.

     #option Explicit

     do
         wait 100 ms

         bytevar = StringToByte( "255" )          ' <<< the StringToByte instruction
         HSerPrint bytevar
         HSerPrintCRLF

         wait 1 s
      loop
    end
----
*Key line:* `bytevar = StringToByte( "255" )` -- parses the decimal digits in the literal string `"255"` and stores the resulting byte value in `bytevar`.

**See Also:**

* <<_stringtolong,StringToLong>> -- related command in the same category
* <<_stringtosingle,StringToSingle>> -- related command in the same category
* <<_bytetostring,ByteToString>> -- the inverse conversion
